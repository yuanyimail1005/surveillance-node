SERVER_HOST=0.0.0.0
SERVER_PORT=5000
SSL_CERT_PATH=./cert.pem
SSL_KEY_PATH=./key.pem

CAMERA_DEVICE=/dev/video0
CAMERA_WIDTH=1920
CAMERA_HEIGHT=1080
CAMERA_FPS=25

SAMPLE_RATE=48000
MIC_CHANNELS=1
SPEAKER_CHANNELS=2
PULSE_SINK_NAME=@DEFAULT_SINK@
PULSE_CAPTURE_SOURCE_NAME=@DEFAULT_SOURCE@
TALKBACK_PLAYBACK_GAIN=5.0

# Face recognition (server-side, using @vladmandic/face-api + @tensorflow/tfjs-node)
# Set FACE_RECOGNITION_ENABLED=true to activate. Keep false to skip loading TF models.
FACE_RECOGNITION_ENABLED=false
# Directory of known faces: one sub-folder per person, containing JPEG/PNG images.
FACE_RECOGNITION_KNOWN_FACES_DIR=./known_faces
# Detect every N frames. Leave unset to auto-calculate (~0.5 s interval at current FPS).
#FACE_RECOGNITION_DETECT_EVERY_N_FRAMES=
# Euclidean distance threshold for a match (lower = stricter). Default: 0.6
FACE_RECOGNITION_MATCH_THRESHOLD=0.6
# Maximum faces to detect per frame.
FACE_RECOGNITION_MAX_FACES=8
# Override the model directory (default: node_modules/@vladmandic/face-api/model).
#FACE_RECOGNITION_MODELS_DIR=
